A simple machine learning project that predicts weather conditions (such as temperature or rainfall) using regression models.
This project uses Python, Pandas, NumPy, Scikit-learn, and Matplotlib for data preprocessing, model training, and visualization.
